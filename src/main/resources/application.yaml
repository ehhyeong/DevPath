server:
  port: 8081 # 서버 포트

spring:
  application:
    name: devpath

  # 스프링이 최상단 루트에 있는 .env 파일을 강제로 읽어오도록 지시
  config:
    import: optional:file:.env[.properties]

  # PostgreSQL 설정
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:devpath_db}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  # sql 자동 실행 옵션
  sql:
    init:
      mode: always

  # JPA 및 하이버네이트 설정
  jpa:
     hibernate:
       ddl-auto: create
     show-sql: true
     defer-datasource-initialization: true  # 🔥 [추가할 부분] JPA 테이블 생성 후 data.sql을 실행하라는 핵심 옵션!
     properties:
       hibernate:
         format_sql: true
         default_batch_fetch_size: 1000

  # Redis 설정
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD}

logging:
  level:
    org.hibernate.SQL: debug